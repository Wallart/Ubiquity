[project]
name = "ubiquity"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "watchdog>=4.0.0",
    "tqdm>=4.0.0",
]

[project.optional-dependencies]
dev = ["pyinstaller>=6.0"]

[project.scripts]
ubiquity = "main:main"
